# Базовый URL бэкенда (оставьте пустым для локальной разработки — используется прокси /api)
VITE_API_URL=

# ID счётчика Яндекс.Метрики (число из "Установка счётчика" в интерфейсе Метрики).
# Без него аналитика просто не подключается — можно оставить пустым.
VITE_YANDEX_METRIKA_ID=
